server:
  port: 8080
  error:
    whitelabel:
      enabled: false

spring:
  profiles:
    active: native
  application:
    name: finwise-config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations

